rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // FULL ACCESS FOR AUTHENTICATED USERS
    // This resolves all permission-denied errors across all collections
    match /{document=**} {
      allow read, write, list: if request.auth != null;
    }
  }
}
